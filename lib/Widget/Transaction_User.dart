import 'package:flutter/material.dart';


// this is change of the transaction when is add or delete
// chart should be here too because when every the add and delete chart also update
class UserTransaction extends StatefulWidget {
  @override
  _UserTransactionState createState() => _UserTransactionState();
}

class _UserTransactionState extends State<UserTransaction> {



  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[

        // i would have the chart and pass the transaction list like Transaction list also since it would change the state
      ],
    );
  }
}


//NewTransaction(),
//Transactionlist(),